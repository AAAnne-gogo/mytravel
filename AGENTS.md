# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository named "mytravel" with no established tech stack, dependencies, or services yet. The repo currently contains only a `README.md`.

- **No build/lint/test commands exist** — there are no `package.json`, `Makefile`, `pyproject.toml`, or any other dependency/config files.
- **No services to start** — no backend, frontend, database, or other services are configured.
- When the project gains a tech stack and dependencies, update this file and the VM update script accordingly.
